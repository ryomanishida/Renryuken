<%= link_to "+", new_admin_item_path %>

<% @items.each do |item| %>
    <%= attachment_image_tag item, :image, :fill, 100, 100 %>
  <%= item.name %><%= link_to "詳細", admin_item_path(item) %>
<% end %>