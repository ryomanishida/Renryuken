<h1>購入履歴</h1>
<% @orders.each do |order| %>
    <%= order.created_at %>
    〒<%= order.postal_code %>
    <%= order.address %>
    <%= order.name %>
    <%= order.total_payment %>円
    <%= order.status %>
    <%= link_to "詳細", order_path(order) %>
    <hr>
<% end %>
